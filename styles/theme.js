const palette = {
  black: '#000000',
};

const colors = {
  text: palette.black,
};

const fonts = {
  default: 'sans-serif',
};

const shadows = {};

export const theme = {
  colors,
  fonts,
  shadows,
};
